# Welcome to your Dyad app

## Price search source

The live product search now runs on the Amazon scraper only.

## Setup

1. Install dependencies with `pnpm install`.
2. Start the app with `pnpm dev`.

No external pricing API key is required.
